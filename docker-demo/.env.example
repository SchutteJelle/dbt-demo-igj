# =============================================================================
# .env.example
# Kopieer dit bestand naar .env en pas de waarden aan.
#   cp .env.example .env
#
# Standaard waarden werken direct voor een lokale demo.
# Voor productie: gebruik sterke wachtwoorden en nooit in versiebeheer!
# =============================================================================

# PostgreSQL credentials
POSTGRES_USER=dbt
POSTGRES_PASSWORD=dbt_demo_2024
POSTGRES_DB=igj_demo

# Poort waarop PostgreSQL bereikbaar is op de host machine
POSTGRES_PORT=5432
